navbar
.container
  .row
    .col-sm-12
      h1 User Profile Settings Go Here.
    .col-lg-3.col-md-4.col-sm-4.col-xs-6
      dl
        dt Name:
        dd {{ ctrl.getCurrentUser().name }}
        dt Login:
        dd {{ ctrl.getCurrentUser().github.login }}
        dt Role:
        dd {{ ctrl.getCurrentUser().role }}
